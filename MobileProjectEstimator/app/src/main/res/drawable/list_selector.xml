<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- I never see this one - the grid items are not focusable -->
    <item android:drawable="@color/dark_blue" android:state_focused="true" android:state_pressed="false"/>

    <!-- while pressed -->
    <item android:drawable="@color/dark_blue" android:state_pressed="true"/>

    <!-- while selected in actionmode -->
    <item android:drawable="@color/light_blue" android:state_activated="true"/>

    <item android:drawable="@color/light_blue"/>
</selector>